---
import SiteLayout from '../layouts/SiteLayout.astro';
import PageHero from '../components/site/PageHero.astro';
import { classrooms } from '../data/classrooms';
import { phone } from '../data/home';

const grades = ['小5', '小6', '中1', '中2', '中3', '高校生'];
---
<SiteLayout
  title="無料相談のお申し込み｜学習塾フォーカス"
  description="学習塾フォーカスの無料相談。教室の雰囲気を知るための気軽な面談です。今の成績や勉強に関するお悩みを、そのままお聞かせください。無理な勧誘は一切いたしません。"
  fixedCta={false}
>
  <PageHero
    eyebrow="CONSULT"
    title="無料相談のお申し込み"
    lead="教室の雰囲気を知るための気軽な面談です。今の成績や勉強に関するお悩みを、そのままお聞かせください。無理な勧誘は一切いたしません。"
  />

  <section class="body">
    <div class="container">
      <form class="form-card" onsubmit="return false">
        <label class="f-field">
          <span>保護者さまのお名前<em>必須</em></span>
          <input type="text" placeholder="例：佐藤 由美" autocomplete="name" required />
        </label>
        <label class="f-field">
          <span>お子さまの学年<em>必須</em></span>
          <select required>
            <option>選択してください</option>
            {grades.map((g) => <option>{g}</option>)}
          </select>
        </label>
        <label class="f-field">
          <span>電話番号<em>必須</em></span>
          <input type="tel" placeholder="090-0000-0000" inputmode="numeric" autocomplete="tel" required />
        </label>
        <label class="f-field">
          <span>ご希望の教室<em>必須</em></span>
          <select required>
            <option>選択してください</option>
            {classrooms.map((c) => <option>{c.name}</option>)}
          </select>
        </label>
        <button type="submit" class="f-submit btn3d btn3d-n" style="background:var(--navy);box-shadow:0 5px 0 var(--navy-shadow)">この内容で無料相談を申し込む</button>
        <p class="f-note">送信後、ご希望の教室からご連絡し、日時を確定します。</p>
        <p class="f-note draft">※送信機能は準備中です（フォームはまだ動作しません）</p>
      </form>

      <div class="tel-block">
        <p class="tel-label">お電話でのお申し込みはこちら</p>
        <a href={phone.tel} class="tel-num">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#E67832" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z"></path></svg>
          {phone.display}
        </a>
        <p class="tel-hours">{phone.hours}</p>
      </div>
    </div>
  </section>
</SiteLayout>

<style>
  .body { background: #fff; }
  .container { max-width: 720px; margin: 0 auto; padding: clamp(36px, 5vw, 56px) 24px; }
  .draft { color: #C0392B; font-weight: 700; }

  .tel-block { text-align: center; margin-top: 34px; }
  .tel-label { font-size: 14px; font-weight: 700; color: var(--ink); margin: 0; }
  .tel-num {
    display: inline-flex; align-items: center; justify-content: center; gap: 8px; margin-top: 6px;
    font-family: var(--font-tel); font-size: clamp(26px, 5.5vw, 30px); font-weight: 800;
    letter-spacing: .02em; color: var(--navy); text-decoration: none;
  }
  .tel-hours { font-size: 12.5px; color: var(--muted); margin: 4px 0 0; }
</style>
