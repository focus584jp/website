---
import SiteLayout from '../layouts/SiteLayout.astro';
import PageHero from '../components/site/PageHero.astro';
import CtaBand from '../components/site/CtaBand.astro';
import SectionHead from '../components/site/SectionHead.astro';
import QuestionBalloon from '../components/site/QuestionBalloon.astro';

// 4セクション構成（2026-08-13 再設計・現行サイト focus584.net/fee/ を参考）:
// 授業料 → 諸経費（まずフォーカスの料金を完結に伝える）→ 他塾との比較 → その他オプション（すべて任意）
// 教科数ごとの月額 = 1教科 6,578円（税込）の単純合算が実料金（2026-08-13 確定）
const plans = [
  { subjects: '1教科', taxed: '6,578' },
  { subjects: '2教科', taxed: '13,156' },
  { subjects: '3教科', taxed: '19,734' },
  { subjects: '5教科', taxed: '32,890' },
];

const subjectChips = [
  { label: '国語', bg: '#B0413E' },
  { label: '数学', bg: '#2C55A0' },
  { label: '英語', bg: '#B95A1E' },
  { label: '理科', bg: '#1F6B3C' },
  { label: '社会', bg: '#6B4FA3' },
];

// オプション（料金は掲載しない。すべて任意であることを本文で明記）
const options = ['定期テスト対策', '過去問演習講座', '外部模試'];

// 他塾比較（現行サイトで公開中の比較表と同データ。1教科あたり・税込）
const compareRows = [
  { label: '基本授業料', m: '20,400円', focus: '6,578円', e: '27,500円', strong: true },
  { label: '入会費', m: '20,000円', focus: '無料', e: '無料', strong: false },
  { label: '設備管理費など', m: '3,200円', focus: '無料', e: '3,520円', strong: false },
];
---
<SiteLayout
  title="料金｜学習塾フォーカス"
  description="学習塾フォーカスの料金。1教科あたり月6,578円（税込）で5教科どれでも同じ料金。入会金・設備管理費0円、授業料は中学3年間据え置きの明朗会計です。"
>
  <PageHero eyebrow="PRICE" title="料金" deco="price" />

  {/* ページの導入となる問い（現行サイトの見出しを踏襲。以降の3セクションが答える構造） */}
  <QuestionBalloon text="個別指導って、高いんじゃないの？" />

  {/* ===== 授業料 ===== */}
  <section class="tuition">
    <div class="container">
      <SectionHead eyebrow="TUITION" title="授業料" />

      <div class="hero-card">
        <p class="label">授業料（1教科あたり）</p>
        <p class="value"><span class="unit">月</span><span class="num">6,578</span><span class="unit">円</span><span class="tax">（税込）</span></p>
        <p class="untaxed">税抜 5,980円</p>
        <div class="chips">
          {subjectChips.map((c) => <span class="chip" style={`background:${c.bg}`}>{c.label}</span>)}
        </div>
        <p class="chips-note">5教科すべて同じ料金です</p>
      </div>

      <table class="plan-table">
        <thead>
          <tr><th>受講教科数</th><th>月額（税込）</th></tr>
        </thead>
        <tbody>
          {plans.map((p) => (
            <tr>
              <td>{p.subjects}</td>
              <td><strong>{p.taxed}円</strong></td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  </section>

  {/* ===== 諸経費 ===== */}
  <section class="fees">
    <div class="container">
      <SectionHead eyebrow="FEES" title="諸経費" />
      <div class="monthly-fee">
        <p class="mf-label">毎月かかる諸経費は</p>
        <p class="mf-value"><span class="mf-name">教材費・システム利用料</span><span class="mf-num">1,375</span><span class="mf-unit">円</span><span class="mf-tax">（税込）</span></p>
        <p class="mf-note">だけ。授業料のほかに毎月かかる費用は、これですべてです。</p>
      </div>
      <div class="fee-cards">
        <div class="fee-card"><p class="fee-name">入会金</p><p class="fee-value">0<span>円</span></p></div>
        <div class="fee-card"><p class="fee-name">設備管理費</p><p class="fee-value">0<span>円</span></p></div>
        <div class="fee-card"><p class="fee-name">授業料の値上げ</p><p class="fee-value">なし<span class="small">（中学3年間据え置き）</span></p></div>
      </div>
    </div>
  </section>

  {/* ===== 他塾との比較（授業料・諸経費を伝えたうえで比較する） ===== */}
  <section class="compare">
    <div class="container">
      <SectionHead eyebrow="COMPARISON" title="他塾との比較" />
      <table class="cmp-table">
        <thead>
          <tr><th></th><th>個別指導<br />M塾</th><th class="focus-col">フォーカス</th><th>個別指導<br />E塾</th></tr>
        </thead>
        <tbody>
          {compareRows.map((r) => (
            <tr>
              <td class="row-label">{r.label}</td>
              <td>{r.m}</td>
              <td class="focus-col">{r.strong ? <strong>{r.focus}</strong> : <span class="free">{r.focus}</span>}</td>
              <td>{r.e}</td>
            </tr>
          ))}
        </tbody>
      </table>
      <p class="cmp-note">※表は1教科あたりの金額（税込）です（当社調べ）</p>
      <p class="cmp-lead">他塾の2教科分の料金で、5教科全部通えます。</p>

      <div class="why">
        <h3 class="sub-h">お手頃価格が実現できるワケ</h3>
        <p class="why-p">一般的な個別指導塾は、正直、高いです。フォーカスは<span class="mk">勉強が苦手な中学生に絞る</span>ことでムダな費用を抑え、それでいて高い授業品質を保っています。安くて高品質だから、長く通い続けられます。</p>
      </div>
    </div>
  </section>

  {/* ===== その他オプション（すべて任意であることを必ず明記） ===== */}
  <section class="options">
    <div class="container">
      <SectionHead eyebrow="OPTIONS" title="その他オプション" />
      <ul class="opt-list">
        {options.map((o) => (
          <li class="opt-item">
            <span class="opt-check"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#143778" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M4 12l6 6L20 6"></path></svg></span>
            {o}
          </li>
        ))}
        <li class="opt-item etc">など</li>
      </ul>
      <p class="opt-p"><span class="mk">オプションはすべて任意です。</span>選ぶ際のアドバイスはいたしますが、必要なサービスを必要な分だけ、保護者さまご自身で都度お選びいただけます。</p>
    </div>
  </section>

  <CtaBand />
</SiteLayout>

<style>
  .container { max-width: 760px; margin: 0 auto; padding: clamp(44px, 6vw, 72px) 24px; }
  .sub-h {
    font-family: var(--font-round); font-weight: 900; font-size: clamp(17px, 2.4vw, 20px);
    color: var(--ink); text-align: center; margin: clamp(36px, 5vw, 48px) 0 18px;
  }

  /* 授業料 */
  .tuition { background: #fff; }
  .hero-card {
    background: var(--blue-paler); border: 2.5px solid var(--navy); border-radius: 16px;
    padding: clamp(24px, 4vw, 34px); text-align: center;
  }
  .label { font-size: 14px; font-weight: 700; color: var(--ink); margin: 0; }
  .value { margin: 6px 0 0; line-height: 1; }
  .num { font: 900 clamp(46px, 9vw, 58px) var(--font-round); color: var(--orange); letter-spacing: .01em; }
  .unit { font: 700 clamp(16px, 3vw, 20px) var(--font-round); color: var(--ink); margin: 0 4px; }
  .tax { font-size: 12px; color: var(--muted); }
  .untaxed { font-size: 12.5px; color: var(--muted); margin: 8px 0 0; }
  .chips { display: flex; justify-content: center; gap: 7px; flex-wrap: wrap; margin-top: 16px; }
  .chip { color: #fff; font-size: 12.5px; font-weight: 700; border-radius: 6px; padding: 4px 11px; }
  .chips-note { font-size: 13px; font-weight: 700; color: var(--ink); margin: 10px 0 0; }

  .plan-table { width: 100%; border-collapse: collapse; margin-top: 28px; }
  .plan-table th, .plan-table td {
    border: 1px solid var(--line); padding: 12px 16px; font-size: 15px; color: var(--ink); text-align: center;
  }
  .plan-table th { background: var(--navy); color: #fff; font-weight: 700; }
  .plan-table strong { font-family: var(--font-round); font-weight: 900; }
  .table-note { font-size: 13.5px; font-weight: 700; color: var(--ink); text-align: center; margin: 12px 0 0; }

  /* 他塾比較（現行サイトの比較表を踏襲: フォーカス列をオレンジ強調） */
  .cmp-table { width: 100%; border-collapse: collapse; }
  .cmp-table th, .cmp-table td {
    border: 1px solid var(--line); padding: 12px 8px; font-size: 14px; color: var(--ink); text-align: center;
  }
  .cmp-table thead th { background: var(--panel); font-weight: 700; line-height: 1.4; }
  .cmp-table thead th.focus-col { background: var(--orange); color: #fff; font-family: var(--font-round); font-weight: 900; }
  .cmp-table td.focus-col { background: #FDEFE4; border-left: 2.5px solid var(--orange); border-right: 2.5px solid var(--orange); }
  .cmp-table tbody tr:last-child td.focus-col { border-bottom: 2.5px solid var(--orange); }
  .cmp-table thead th.focus-col { border: 2.5px solid var(--orange); border-bottom: none; }
  .cmp-table td.focus-col strong { font: 900 clamp(18px, 3.4vw, 22px) var(--font-round); color: var(--orange); }
  .cmp-table td.focus-col .free { font: 900 15.5px var(--font-round); color: var(--ink); }
  .cmp-table .row-label { background: var(--panel); font-weight: 700; font-size: 13.5px; }
  .cmp-note { font-size: 12px; color: var(--gray-1); margin: 10px 0 0; line-height: 1.7; }

  .why-p { font-size: 15px; line-height: 2.1; color: var(--ink); margin: 0 auto; max-width: 640px; }

  /* 諸経費 */
  .fees { background: var(--alt-bg); }
  .monthly-fee {
    background: #fff; border-radius: 14px; box-shadow: var(--shadow-card);
    padding: clamp(20px, 3.5vw, 28px); text-align: center; margin-bottom: 14px;
  }
  .mf-label { font-size: 14px; font-weight: 700; color: var(--ink); margin: 0; }
  .mf-value { margin: 8px 0 0; line-height: 1.3; }
  .mf-name { display: block; font-size: 13.5px; font-weight: 700; color: var(--muted); margin-bottom: 2px; }
  .mf-num { font: 900 clamp(34px, 7vw, 42px) var(--font-round); color: var(--navy); }
  .mf-unit { font: 700 16px var(--font-round); color: var(--ink); margin-left: 3px; }
  .mf-tax { font-size: 12px; color: var(--muted); margin-left: 2px; }
  .mf-note { font-size: 13.5px; font-weight: 700; color: var(--ink); margin: 10px 0 0; line-height: 1.9; }
  .fee-cards { display: flex; flex-direction: column; gap: 12px; }
  .fee-card {
    background: #fff; border-radius: 14px; box-shadow: var(--shadow-card);
    padding: 18px 20px; display: flex; align-items: center; justify-content: space-between;
  }
  .fee-name { font-size: 15px; font-weight: 700; color: var(--ink); margin: 0; }
  .fee-value { font: 900 26px var(--font-round); color: var(--navy); margin: 0; }
  .fee-value span { font-size: 15px; }
  .fee-value .small { font-size: 12px; font-weight: 700; color: var(--muted); margin-left: 4px; }
  @media (min-width: 768px) {
    .fee-cards { flex-direction: row; }
    .fee-card { flex: 1; flex-direction: column; gap: 8px; justify-content: center; }
  }

  /* 他塾との比較 */
  .compare { background: #fff; }
  .cmp-lead { font-size: 15px; font-weight: 800; color: var(--ink); text-align: center; margin: 18px 0 0; }

  /* その他オプション */
  .options { background: var(--alt-bg); }
  .opt-list { list-style: none; margin: 0 auto 20px; padding: 0; max-width: 480px; display: flex; flex-direction: column; gap: 10px; }
  .opt-item {
    display: flex; align-items: center; gap: 10px;
    background: #fff; border-radius: 12px; box-shadow: var(--shadow-card);
    padding: 13px 16px; font-size: 15px; font-weight: 700; color: var(--ink);
  }
  .opt-item.etc { background: none; box-shadow: none; justify-content: center; color: var(--muted); font-size: 13.5px; padding: 0; }
  .opt-check {
    flex: none; width: 26px; height: 26px; border-radius: 50%;
    background: #E3ECFB; display: grid; place-items: center;
  }
  .opt-p { font-size: 15px; line-height: 2.1; color: var(--ink); margin: 0 auto; max-width: 640px; }
</style>
